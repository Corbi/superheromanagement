CREATE TABLE SUPERHEROS (
    ID int,
    NAME varchar(100),
    POWER varchar(100),
    TEAM int,
    PRIMARY KEY (ID)
);

CREATE TABLE TEAMS (
    ID int,
    NAME varchar(100),
    PRIMARY KEY (ID)
);